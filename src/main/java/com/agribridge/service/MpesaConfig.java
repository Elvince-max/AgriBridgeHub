package com.agribridge.service;

public class MpesaConfig {
    // Set to true to simulate payment (no real M-Pesa call)
    public static final boolean SIMULATE = true;
    
    // You can add other constants later (consumer key, secret, etc.)
}